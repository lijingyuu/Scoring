<!-- markdownlint-disable -->

# Development Log

## 2026-06-06 排球模块第一阶段

这阶段的总方向先定清楚了：后端赛事体系尽量复用，前端尤其是排球部分重新设计。目标不是做一个简易记分器，而是先打通一套能用于真实校园比赛记录的最小闭环。

	- 赛事大厅的方案来回想过，最后没有拆成羽毛球大厅和排球大厅，而是共享一个大厅，在列表里明确标记 `sportType`。创建比赛时先选运动类型，再进入各自流程。
	- 排球创建页没有复用羽毛球原来的“选手报名”方式，而是改成逐支添加队伍。每队录入最多 12 人，包含姓名、号码、自由人、队长。排球第一阶段只支持淘汰赛。

计分页布局中途被重新校正过一次，这次调整很关键。最后定下来的是横屏三栏：

	- 左边整列左队完整名单
	- 中间上面比分牌
	- 中间下面模拟小球场
	- 右边整列右队完整名单

这里的重点不是比分占满页面，而是“名单 + 比分 + 小球场”协同。

	- 首发设置流程也专门收敛过，没有放在创建比赛时做，而是改成进入具体排球比赛后，先完成双方首发 6 人、初始站位和初始发球方设置，再进入正式计分页。这样更贴近真实比赛流程。
	- 换人交互思路也定了：优先走“先点名单里的球员，再点小球场目标位置”的路径。第一阶段先支持普通球员换人和自由人参与换人，不上复杂规则引擎。

实现上已经把排球主链路搭起来了，当前已完成的核心点包括：

	- 新增 `sportType`
	- 排球独立创建页
	- 后端 `teams` 结构
	- 赛事大厅和详情页识别排球
	- 赛程页按运动类型分流
	- 排球首发设置页
	- 排球专用计分页
	- 排球队员独立落表
	- 排球比赛本地状态管理

最近联调时发现过一个真实问题：排球添加了队伍后，创建比赛仍然报“选手列表不能为空”。本质上是请求误走了羽毛球旧校验链路。后来后端做了兼容修复，只要请求里有 `teams` 且没有 `players`，就直接按排球处理，不再只死看 `sportType`。

当前项目状态已经不是“准备支持排球”，而是排球第一阶段的产品结构和主链路都已经成立，重点开始转向联调稳定性、交互顺手度、页面细节和现场操作感。

后面继续开发时，脑子里保持几个原则就行：

	- 大厅共享，不拆分
	- 排球是球队模型，不是选手模型
	- 首发在赛前设置
	- 计分页核心是三栏结构
	- 第一阶段先求能用、清晰、顺手，不先做重规则系统

    
## 2026-06-09 排球记分页第二阶段

这一阶段真正把排球记分牌从“能跑通”推进到了“现场可操作”。最关键的架构判断是：微信小程序里不要指望在同一页面里先竖屏再横屏动态切换，所以把赛前轮次填写和正式比赛记分彻底拆成两个页面。竖屏页只负责轮次填写，横屏页只负责比赛过程，这样方向逻辑、页面职责、后续调试都清楚很多。

拆分之后，排球的主链路已经稳定成下面这套：
	- 比赛树点击排球比赛，先进竖屏轮次填写页
	- 两支队伍分别填写轮次，确认首发球方
	- 点“开始比赛”再跳横屏正式记分页
	- 每小局结束后，如果整场还没结束，会自动回竖屏页重新确认下一局轮次
	- 下一局默认沿用上一局轮次，默认发球方自动切到另一边
	- “重新开始”不在横屏页里回退成编辑态，而是清状态后回竖屏重新来

用户这轮反复纠正过一个很关键的细节：轮次填写页里 6 个站位必须是稳定的两行三列，不接受视觉上像两列半、右边空一块的混乱布局。后来把这一块改成真正的 grid，顺序固定为第一行 `4/3/2`、第二行 `5/6/1`。这类问题表面看是 CSS，小本质其实是“赛场操作页的瞬时可读性”。

正式比赛横屏页后面又被继续压了一轮，方向也被重新校正了。最终定下来的不是一味整页缩小，而是“左右名单先收缩、中间比赛区优先稳定”：
	- 左右名单栏允许明显变窄，极限能压到约 130px
	- 左右名单栏允许独立滚动
	- 中间栏不允许滚动，比分和轮转区优先保住
	- 顶部 pills、比分卡、轮转格子、按钮高度都开始用连续收缩的方式处理

比分区还专门收过一次高度。原来“撤销 / 暂停 / 退赛”都堆在中间列里，占了不少竖向空间。后来改成顶部一排挂“撤销 / 退赛”，中间只留一个“暂停”，这样比分卡更扁，轮转区能拿到更多高度。这类调整的经验是：想压高度时，优先挪操作位次，不要第一反应就是缩字。

微信开发者工具这边暴露了一个很真实的问题，不是渲染坏了，而是本地存储被打爆。控制台报的是 `setStorageSync:fail exceed storage max size 10Mb`，顺着查下来发现根因不是普通“存太多”，而是撤销历史做成了套娃。之前 `pushHistory()` 往历史里塞的是“完整比赛快照”，而完整快照里又带着 `historyStack`，所以会变成“历史里嵌历史”，体积膨胀非常快。后来收成三条原则：
	- 历史条目只保存纯业务快照，不再带历史本身
	- 历史条数加上限，默认保留 40 条
	- 存储写入失败时退化成清空撤销历史后再落盘

这一轮也顺手清掉了一批历史乱码字符串。它们不是全局编码配置坏掉，而是一些旧中文文案在搬动和重写时残留成了乱码。把“发球 / 队长 / 场上轮转 / 第几局 / 主队客队 / 错误提示”这些页面里真正看得到的文案清理完之后，界面的完成度终于开始像一个正式产品页，而不是半成品调试页。

当前阶段最值得记住的，不是某个具体像素值，而是几条已经被反复验证过的原则：
	- 微信小程序里，方向切换优先靠页面拆分，不要在同页里硬做两套方向语义
	- 赛前填写和赛中操作必须分层，职责越清楚，后面越不容易爆
	- 排球记分页是控制台，不是普通信息页，结构稳定比局部华丽更重要
	- 小程序联调时，要优先盯本地存储、滚动边界、按钮层级这类“现场问题”
	- 撤销历史这种东西一旦要跨页持久化，必须先防套娃和防无限膨胀

## 2026-06-10 自由人模块收口

这一轮真正把自由人从“队员静态标签”改成了“每局轮次配置 + 记分页本地联动”。最先定下来的原则是：创建比赛时不再指定谁是自由人，自由人要在进入某一局的轮次填写页时再确认；后端只保存局前配置，不追踪比赛中的实时轮转和自动换人。

轮次填写页这边改了两层。第一层是在首发 6 个位置下面加“开始添加自由人 / 重新设置自由人”，前提是 6 个位置必须先填满；第二层是把“副攻识别”和“自由人绑定”拆成两步：先在 `4/3/2, 5/6/1` 这个固定两行三列布局里点一个位置，系统自动拿它的对角位组成一对副攻，再从下方剩余队员里给“自由人1 / 自由人2”选人。这里特意保留了用户反复强调的规则：自由人可以不绑、可以单绑、可以双绑，也允许同一个人同时绑两个副攻。

后端没有沿用旧的 `libero/is_libero` 语义，而是单独加了 `matchId + gameNo` 维度的 `lineup-config`。核心思路不是记录整场比赛谁天生是自由人，而是保存“这一局开始前双方确认的轮次配置”：首发 6 人、两组副攻对角位索引、每组绑定的自由人 ID、发球方。读取时支持向前继承上一局配置，保存时同局覆盖，但已结束的局锁定不可改。这样前端下一局默认沿用上一局配置的能力，就不再只靠本地缓存硬撑。

真正最费劲的是记分页里的自动自由人联动。前面一度把逻辑想偏了，后来被用户连续纠正了几次，最终必须死守这套场上认知：前排永远是 `4/3/2`，后排永远是 `5/6/1`，自动换人的关键节点只有两个：到 `4号位` 必须换回副攻，到 `1号位` 时允许副攻本人先发球，但一旦失去发球权，就要在 `1号位` 立刻切成绑定自由人。这里最重要的不是“某个球员是谁”，而是“这一对对角副攻当前谁在前排、谁在后排”。

实现上最后没有再重做一整套大状态机，而是在现有 `scoreboard.vue + match-state.js` 本地状态模型上加了一层运行态：`leftLiberoSetup/rightLiberoSetup` 保存局前绑定关系，`leftLiberoRuntime/rightLiberoRuntime` 保存这对副攻当前落在哪两个槽位、各自当前对应的真实副攻持有人。记分时如果接发方得分，先做真实轮转，再推进运行态槽位，再按规则结算自由人是否上场；如果发球队丢分，则不轮转，但要补做一次 `1号位` 失发球权后的自动自由人结算。手动换人后也要立刻重算持有人，避免后面从前排换回时又错误地换回原始首发。

联调里暴露出的 bug 也很真实。最早的问题是“副攻发完球失去发球权后，自由人不是立刻换上，而是要再拖几轮”；往下查发现至少混着两层问题：一层是内部 6 槽位轮转顺序和页面展示顺序没有完全对齐，另一层是自由人运行态在某些情况下会把绑定自由人误认成普通持有人，最后造成“自由人到了前排不换回”“同一个自由人同时出现在两个位置”这种现象。

后面的修正重点不是继续拍脑袋调 if，而是把几个关键约束补全：
	- 轮转和显示统一按 `4/3/2, 5/6/1` 这套槽位认知处理，不能再拿 UI 的二维位置和内部数组顺序混着算。
	- 自由人运行态要做合法性校验，旧缓存或错误状态一旦不自洽，就直接重建，不能让脏状态一直在本地持久化里传下去。
	- 同一个自由人双绑时，不应该被当成两个独立物理人去占两个位置；本质上他仍然只能作为“当前应该替补上场的那个后排副攻”的自由人身份出现一次。

这一轮收口之后，当前效果已经比较接近用户想要的现场操作感了：自动自由人联动不再依赖旧的静态自由人字段，按每局配置生效；到 `4号位` 和 `1号位失去发球权` 这两个节点时能按预期换人；自由人上场后，记分页里的号码还会额外用橙色显示，方便裁判或操作者一眼识别现在这个位置上站的是自由人，不是副攻本人。

后面如果继续扩这个模块，脑子里先记住三件事就够了：
	- 自由人配置属于“每局局前配置”，不是队员静态属性。
	- 自动联动是前端本地规则，不是后端实时比赛日志。
	- 排查 bug 时先统一“场上 1-6 号位认知”，再看状态机；这个模块里一旦位置认知偏了，后面所有判断都会一起歪掉。

## 2026-06-10 场上队长与赛事事件流收口

这一轮没有把“场上队长”当成一个孤零零的弹窗功能去补，而是顺手把比赛过程里最小可用的事件流骨架一起搭起来了。方向上先收得很克制：本轮不做每球事件化，不碰过重的规则引擎，只把赛后记录真正需要的几类过程数据先站住，包括开场轮次快照、暂停、手动换人、场上队长变更。

场上队长的规则这次算是正式定型了。队伍原始队长仍然来自现有队员 `captain` 字段，但“当前场上队长”变成了记分页运行态。进入记分牌后按左、右顺序检查当前在场 6 人：如果原始队长在场，就自动设为场上队长；如果不在场，就强制弹出确认。比赛过程中如果当前场上队长被换下，必须先记录换人，再立刻阻断后续记分，要求重新确认场上队长；如果原始队长后来重新回场，则自动恢复为场上队长。这套闭环很重要，因为它把“谁是队长”从静态标签改成了跟比赛过程真实联动的运行态。

后端这边没有把过程数据硬塞回旧的 `match_record`，而是单独补了一张比赛事件表，按 `matchId + eventSeq` 做幂等。前端本地则同步加了事件队列、`lastSyncedEventSeq`、补传与结算前兜底同步。实现思路还是尽量朴素：先更新本地比赛状态，再把事件落本地，再异步尝试补传；如果补传失败，不阻塞现场操作，但结算前必须先追平事件同步。这轮最值得记住的不是接口名字，而是这个取舍本身：先保证现场能继续记，再保证赛后能追出足够完整的记录。

联动上实际落地的事件类型目前先收成四类：`lineup_snapshot`、`timeout`、`substitution`、`captain_change`。其中手动换人只记录 `side / outMemberId / inMemberId`，不额外展开过多语义；场上队长变更则区分自动与手动来源，但来源信息先只保存在事件里，前台暂时不展示。这个边界收得对，避免这轮就把赛事记录系统做成一个过度复杂的通用日志平台。

前端交互这轮也被反复校正了几次，最后的方向比最开始清楚得多。队长确认最初想只覆盖中间上半部分，后来改成全屏遮罩，但要求底下确认按钮必须稳定露出来；候选人最开始是流式排布，结果在 pad 上跑成第一行 5 个、第二行 1 个，视觉非常乱，最后收成真正固定的两行三列，并明确顺序是第一行 `4/3/2`、第二行 `5/6/1`。这件事再次说明：赛场操作页里，结构稳定和瞬时可读性比“自适应看起来灵活”重要得多。

视觉状态也顺手收了一轮。场上队长新增了一套青蓝色标识，不再复用原来的橙色体系；名单里保留“队长”字样表示原始队长身份，但当前场上队长不再额外显示“场上”标签，而是改成名字和外框直接转青蓝色。场下替补则把旧的橙色边框和橙色序号一起清掉，避免名单区同时混着“在场高亮 / 自由人 / 场上队长”三套互相打架的颜色语义。到这一步，页面信息层级终于开始顺了。

微信开发者工具那次看起来像“队长弹层渲染炸了”的问题，后面证明其实不是逻辑 bug，而是旧缓存和旧构建产物干扰。网页端正常、小程序端异常时，第一反应不能只怀疑代码分支，还得先确认 mp-weixin 是否重新构建、开发者工具是否加载了最新包、本地缓存是否清干净。这次图里出现过一次 `[object Object]` 文案，本质上也提醒了后面联调要先分清“渲染差异”“缓存污染”“真实数据类型错误”到底是哪一类问题，别在错误方向上白花时间。

这一轮收完之后，排球记分页这块已经明显从“功能能跑”进入“模块基本成型”的状态了。真正搭起来的不是某个弹窗，而是一条更完整的现场操作链：开场自动/手动确认场上队长，比赛中手动换人触发队长重选，过程事件本地落盘并可补传，赛后具备生成最小赛事记录的基础数据。后面再继续扩时，优先级就不该再是胡乱加功能，而应该转向验主链、补细节和做 pad 观感抛光。


## 2026-06-10 排球 Pad 适配与小组赛复用落地

这一轮最大的判断，是把排球 Pad 适配从“手机横屏放大版”彻底改成“按真实窗口尺寸驱动的平板布局”。方向没有改：`lineup.vue` 继续固定竖屏，`scoreboard.vue` 继续固定横屏；改的是成立方式，不再依赖固定画布脑补大屏观感，而是直接用 `uni.getWindowInfo()` 和 `uni.onWindowResize()` 拿真实可用尺寸，再落到几个很轻的运行态：`windowWidth`、`windowHeight`、`isTablet`、`orientation`、`sizeBand`。这次刻意没引入复杂响应式系统，只让页面根节点挂 class、关键尺寸走 CSS 变量、少数位置按 band 分段兜底，保持实现简单可控。

Pad 横屏记分页的取舍这次也算彻底定了。用户明确要“大屏尽量铺满，但不要摊成一片空”，所以最后不是居中窄容器，也不是所有东西一起等比放大，而是继续坚持“左右名单先让、中间比赛区优先吃空间”。后面又顺着这个方向补了一次微调：两侧名单再略收一点，中间比分和轮转再放大一点，字体整体也往上提一档。现在的倾向已经比较明确：
	- 小尺寸 Pad 横屏先把名单栏抬到手机之上，但仍保持克制，不让它反客为主。
	- 中尺寸 Pad 横屏让中间比分区、按钮高度、轮转卡片一起长大，三栏仍然同屏同高。
	- 大尺寸 Pad 横屏继续铺满，但单个元素加上限，不让大比分、按钮、卡片无限发胖。

竖屏 `lineup.vue` 这次的思路也和手机版彻底分开了。主页面不再只是单列内容放大，而是改成更像平板的双入口大卡布局；编辑页则不再坚持“上站位、下名单”的手机流，而是改成左边站位与自由人配置、右边队员名单滚动区的双区同屏。这个决定本质上是为了减少局间重复上下滚动，让排阵这件事在 Pad 上真正像一个工作台，而不是把手机表单简单拉伸。

配置边界也在这一轮定死了：[manifest.json](/D:/LJY/grade2/Scoring/src/manifest.json:54) 里给 `mp-weixin` 开了 `resizable: true`，后续只按微信小程序 Pad 全屏场景验收，不专门照顾分屏和悬浮窗。这里最值得记住的不是某个具体断点，而是一个经验：Pad 适配不要先追求“抽象设备类型”，而要优先围绕真实 `windowWidth × windowHeight` 的几个主流可用区间做视觉裁定，尤其是横屏控制台这类页面。

除了 Pad，这一轮还顺手把排球小组赛这条链真正打通了，而且策略上不是另起一套排球专用赛制，而是尽量复用羽毛球现有的小组赛骨架。后端原来把排球锁死在淘汰赛里，这次改成创建排球赛事时同样走 `tournamentType`，如果是小组赛就先分组，再生成组内循环赛程，再衔接后续淘汰。这个决策很值，因为它说明“排球和羽毛球规则不同”不代表“赛事编排骨架也要拆两套”，能共用的先共用，真正不同的只留给比赛过程页。

前端对应也做了收口，而且不是零碎补洞，而是把几个关键页面直接拉回了干净状态。排球建赛页现在已经支持小组赛参数，前端会先校验“每组人数必须大于出线人数”，跟后端口径对齐；赛事详情页如果是小组赛，不管羽毛球还是排球，都统一跳分组页；分组页里再按 `sportType` 分流点击行为，羽毛球走原来的记分页，排球则进排阵页，再从排阵页进正式记分。这样一来，排球小组赛主链没有另开一条怪路线，而是尽量挂回现有赛事大厅和详情体系里。

测试结论这次也要记清楚，避免后面自己把话说满：当前已经确认的是前端 `npm run build:h5` 能过，后端和本轮改动直接相关的 `TournamentControllerIntegrationTest` 能过，里面还专门补了排球小组赛建赛场景；但后端全量 `mvn test` 这一轮还没有完整跑完，所以现阶段不能写成“所有后端接口测试全部通过”。这条事实要记住，后面再继续联调或回归时，别自己把局部通过说成全盘稳了。

这一轮收口之后，排球模块的状态比前面清楚很多了：Pad 适配已经从方向争论进入 token 微调阶段；排球小组赛已经不再停留在“以后可以做”，而是和羽毛球共骨架地真正落地；接下来优先级应该是继续验主链、跑回归、把 Pad 真机观感和小组赛细节打磨稳，而不是再扩新的大功能。


