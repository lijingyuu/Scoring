# 测试策略 — 测试指南

> 跑测方法、测试架构、覆盖现状与手动验收。
> 关联：[[架构.md]] · [[数据库.md]]

---

## 1. 运行测试

### 后端

```bash
cd backend

# 全量测试
mvn test

# 指定单个测试类
mvn -Dtest=BracketEngineTest test

# 指定一组测试
mvn "-Dtest=MatchWriteAuthIntegrationTest,MatchLockIntegrationTest,MatchEventIntegrationTest" test

# 跳过集成测试，只跑单元测试
mvn test -DexcludedGroups=integration
```

当前源码静态统计：**243 个后端测试声明**（按 `@Test` / `@ParameterizedTest` 统计）。实际通过情况以本地执行 `mvn test` 为准；`MatchThemeConfigIntegrationTest` 因配色接口废弃保持禁用。

### 前端

```bash
cd frontend
npm test                 # 全部前端单元测试（vitest）
npm run test:watch       # watch 模式
```

当前源码静态统计：**235 个前端用例**（13 个测试文件）。实际通过情况以本地执行 `cd frontend && npm test` 为准。
测试框架：vitest + jsdom + `@vue/test-utils`。
配置：`frontend/vitest.config.js`，路径别名 `@` → `frontend/src/`。

---

## 2. 测试架构

### 2.1 分类

| 类型 | 位置 | 特点 |
|------|------|------|
| **单元测试（白盒）** | `*Test.java` | 纯 Java，不启动 Spring，使用 Mockito mock 依赖 |
| **集成测试（黑盒）** | `*IntegrationTest.java` | `@SpringBootTest` + H2 内存库 + `MockMvc` |
| **前端单元测试** | `frontend/src/**/*.test.js` | 纯函数测试，vitest |

### 2.2 集成测试配置

所有集成测试共享以下模式：

```java
@SpringBootTest(classes = ScoringBackendApplication.class)
@AutoConfigureMockMvc
@TestPropertySource(properties = {
    "spring.datasource.url=jdbc:h2:mem:<unique_db_name>;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE",
    "spring.datasource.driver-class-name=org.h2.Driver",
    "spring.flyway.enabled=false",                    // 禁用 Flyway
    "spring.sql.init.mode=always",
    "spring.sql.init.schema-locations=classpath:schema-h2.sql",  // 用 H2 兼容 schema
    "app.rate-limit.enabled=false",                   // 禁用限流
    "app.auth.jwt-secret=test-secret"
})
```

关键约定：
- **每个集成测试类用独立的 H2 数据库名**（`DB_CLOSE_DELAY=-1` 保持连接），避免测试间数据污染
- **Flyway 禁用**，改用 `schema-h2.sql` 直接建表（H2 不完全兼容 MySQL DDL）
- **AuthService 用 `@MockBean` mock**，`verifyToken` 返回固定 userId
- **限流器禁用**，避免干扰功能测试

### 2.3 测试辅助工具

```java
// Mock AuthService 模拟已登录用户
@MockBean
private AuthService authService;

@BeforeEach
void setUp() {
    when(authService.verifyToken(anyString())).thenReturn("user-1");
}

// 构建测试用户
private User buildUser(String id, String openid, boolean profileCompleted) {
    User user = new User();
    user.setId(id);
    user.setOpenid(openid);
    user.setNickname(id);
    user.setAvatarUrl("https://example.com/avatar.png");
    user.setProfileCompleted(profileCompleted);
    return user;
}
```

---

## 3. 测试覆盖现状

### 3.1 后端覆盖（2026-09 实测，`mvn test` 共 303 通过 + 5 跳过；源码静态统计约 326 个测试声明，含参数化展开）

**单元测试（白盒）**

| 模块 | 测试数 | 覆盖质量 | 类型 |
|------|--------|---------|------|
| `RoundRobinEngine` | 21 | ⭐⭐⭐⭐ | 单元 |
| `BracketEngine` | 15 | ⭐⭐⭐⭐⭐ | 单元 |
| `GroupStandingEngine` | 19 | ⭐⭐⭐⭐ | 单元 |
| `MatchServiceImplTest`（门面 + 真实协作者挂 mock） | 23 | ⭐⭐⭐⭐ | 单元 |
| `AuthServiceImpl` (Mock) | 14 | ⭐⭐⭐⭐ | 单元 |
| `TournamentService` (Mock) | 6 | ⭐⭐ | 单元 |
| `UserServiceImpl` (Mock) | 6 | ⭐⭐⭐⭐ | 单元 |
| `TournamentRuleResolver` | 1 | ⭐⭐ | 单元 |
| `FileServiceImpl` | 1 | ⭐⭐ | 单元 |
| `ApiResponse` | 3 | ⭐⭐ | 单元 |
| `GlobalExceptionHandler` | 3 | ⭐⭐ | 单元 |
| `ProductionStartupChecker` | 3 | ⭐⭐ | 单元 |
| `ClientIpResolver` | 2 | ⭐⭐ | 单元 |

**集成测试（MockMvc + H2）**

| 模块 | 测试数 | 覆盖质量 | 类型 |
|------|--------|---------|------|
| `TournamentController` | 45 | ⭐⭐⭐⭐⭐ | 集成 |
| `RoundRobin` | 17 | ⭐⭐⭐⭐ | 集成 |
| `BadmintonTeamTournament` | 21 | ⭐⭐⭐⭐ | 集成 |
| `MatchWriteAuth` | 14 | ⭐⭐⭐⭐⭐ | 集成 |
| `MatchLock`（执裁互斥锁） | 16 | ⭐⭐⭐⭐⭐ | 集成 |
| `MatchFinish` | 12 | ⭐⭐⭐⭐⭐ | 集成 |
| `RoundRule` | 8 | ⭐⭐⭐⭐ | 集成 |
| `MatchLineupConfig` | 8 | ⭐⭐⭐⭐⭐ | 集成 |
| `TournamentFavorite` | 7 | ⭐⭐⭐⭐ | 集成 |
| `TournamentArchive` | 5 | ⭐⭐⭐⭐ | 集成 |
| `RoundRobinTournament` | 5 | ⭐⭐⭐⭐ | 集成 |
| `MatchThemeConfig` | 5 | ⛔ 已禁用 | 集成 |
| `MatchEvent` | 3 | ⭐⭐⭐⭐ | 集成 |
| `AuthControllerUpload` | 1 | ⭐⭐⭐ | 集成 |
| `RequestRateLimit` | 1 | ⭐⭐⭐ | 集成 |
| `PcLogin`（网页扫码登录票据状态机，含非扫码人替确认拦截） | 11 | ⭐⭐⭐⭐⭐ | 集成 |

### 3.2 前端覆盖

| 模块 | 测试数 | 状态 |
|------|--------|------|
| `match-state.js` | 40 | ✅ — 状态创建/归一化/换边/旧缓存迁移/边界值 |
| `relay-scoring.js` | 76 | ✅ — 接力分段计算/链条验证/段切换/分数快照 |
| `tournament-navigation.js` | 15 | ✅ — 路由归一化/页面栈遍历/跳转解析 |
| `groups-data.js` | 4 | ✅ — 小组赛数据聚合/排名计算 |
| `volleyball-team.js` | 8 | ✅ — 排序逻辑/队长查找/空输入 |
| `interaction-guard.js` | 10 | ✅ — useDelayedTapGate 时序/useActionLock 锁定与释放 |
| `match-guard.js` | 7 | ✅ — requireMatchOperator 权限守卫 |
| `match-lock.js` | 15 | ✅ — token 生成/header/acquire·heartbeat·release 请求形状/心跳定时·失锁判定/网络错误容忍/stop |
| `score-recovery.js` | 34 | ✅ — 排球恢复纯函数：局胜判定(封顶/决胜/金球/非金球)/后端恢复计算/saved vs score 进度/缓存重建 |
| `request.js` | 0 | ❌ — 需要 mock uni API，待补 |

---

## 4. 已知问题

| 问题 | 说明 |
|------|------|
| `MatchThemeConfigIntegrationTest` 被 `@Disabled` | 配色接口 `PUT/GET /theme-config` 已废弃（前端改为硬编码），测试随接口一起禁用 |
| 后端测试统计 | 285 个通过 + 5 跳过（`mvn clean test`，2026-09 实测） |
| 前端测试已配置 vitest | ✅ `npm test` 可用；13 个测试文件、255 个用例（2026-09 实测） |
| 集成测试 mock 了 AuthService | 真实微信 code→openid→JWT 链路未覆盖，需 E2E 测试或手动验证 |

---

## 5. 补测优先级路线图

### ✅ 本轮已完成（P0 + P1 全覆盖）

**P0 后端白盒**
- `MatchServiceImplTest` — finishMatch 7种校验 + 晋级 + 退赛 + 事件幂等 → 12 用例
  （2026-09 拆分后：门面注入真实 `MatchAccessGuard`/`MatchLockService`/`MatchReportAssembler`/`MatchDetailAssembler`/`MatchSettlementService`，mock 只挂 Mapper 层）
- `AuthServiceImplTest` — JWT 签发/验证 + mock openid + token 篡改 → 7 用例
- `UserServiceImplTest` — 获取/更新 profile + trim + 用户不存在 → 6 用例

**P0 前端纯函数**
- `match-state.test.js` — 状态归一化/换边/旧缓存迁移/URL → 40 用例
- `volleyball-team.test.js` — 排序/队长查找/空输入 → 8 用例
- `interaction-guard.test.js` — useDelayedTapGate 时序/useActionLock → 10 用例
- vitest + `npm test` 脚本 → 前端测试基础设施

**P1 后端集成测试**
- `MatchFinishIntegrationTest` — finishMatch 正常3:1 + 决赛结束赛事 + 退赛 + 负局分拒绝 + wrongWinnerSide + 平局拒绝 + winnerMismatch + 缺gameScores → 8 用例
- `MatchLockIntegrationTest` — 执裁独占锁：互斥接管/过期接管/创建者接管/心跳续期/写守卫 403(过期·持有者不匹配)/finish·restart 终态清锁/report-meta 不受守卫/同 token 幂等重进 → 13 用例
- `TournamentFavoriteIntegrationTest` — 收藏/幂等/取消/我的收藏/空列表/我创建的/多用户独立 → 7 用例
- `RoundRobinEngineTest` 补全 — 3人/6人/ID唯一性/roundNum+stageType/每轮分布/1人抛异常/空列表抛异常 → 当前 21 用例

### 📋 待补（后续）

| 优先级 | 模块 | 预计测试数 | 难度 |
|--------|------|-----------|------|
| P1 | `buildRotationGrid` 单元测试 | ~8 | 高（300行复杂逻辑） |
| P1 | TeamMatchServiceImpl 单元测试 | ~10 | 中 |
| P1 | 新协作者（MatchLockService/MatchSettlementService/TournamentCreationFactory）单元测试 | ~15 | 中 |
| P1 | 团体赛回合集成测试 | ~8 | 中 |
| P2 | `request.js` | ~5 | 高（需 mock uni API） |
| P2 | 接力赛记分板集成测试 | ~6 | 中 |
| P2 | E2E 测试（微信登录→创建赛事→记分→结算） | ~3 | 高 |

---

## 6. 编写测试约定

### 单元测试

```java
// 命名: <被测类名>Test.java
// 方法命名: <方法名>_<场景>_<预期结果>
// 使用 @ExtendWith(MockitoExtension.class)
// 只 mock 外部依赖（Mapper），不 mock 被测类

@Test
void finishMatch_withValidRequest_shouldUpdateMatchAndPropagateWinner() {
    // Given
    // When
    // Then
}
```

### 集成测试

```java
// 命名: <被测模块>IntegrationTest.java
// 每个测试独立准备数据（@BeforeEach 清理 + 插入）
// 使用 MockMvc 模拟 HTTP 请求
// 用 jsonPath 断言响应
// 用 Mapper 直接读数据库断言副作用
```

### 前端测试（规划）

```js
// 命名: <被测模块>.test.js
// 使用 vitest + jsdom
// 只测纯函数，不测 Vue 组件渲染
```

---

## 7. 测试数据清理

集成测试使用 H2 内存数据库，每次 `@BeforeEach` 显式删除所有表数据：

```java
@BeforeEach
void setUp() {
    matchEventMapper.delete(new QueryWrapper<>());
    matchLineupConfigMapper.delete(new QueryWrapper<>());
    matchRecordMapper.delete(new QueryWrapper<>());
    tournamentTeamMemberMapper.delete(new QueryWrapper<>());
    playerMapper.delete(new QueryWrapper<>());
    tournamentMapper.delete(new QueryWrapper<>());
    userMapper.delete(new QueryWrapper<>());
    // ... 插入基础测试数据
}
```

> 注意：删除顺序必须从子表到父表（外键依赖），或使用 H2 的 `SET REFERENTIAL_INTEGRITY FALSE`。

## 8. 小组赛排名模板的最小验证

新增默认模板时，至少验证以下闭环：

1. 前端排名模式选项只包含当前赛制允许的 criterion，并排除其他赛制的层级指标。
2. 前端小组排名表只显示当前模板对应的统计列。
3. 后端 `RankingConfig.preset()` 的优先级、数学类型、退赛策略和 JSON 回显一致。
4. `GroupStandingEngineTest` 覆盖模板优先级；若涉及多队并列，增加实际比分排序用例。
5. 若模板通过创建页或自定义页保存，补一个创建/更新后 GET 回显的集成验证。

排名模板变更的最小命令示例：

```powershell
cd frontend
npm test -- src/pages/ranking/ranking-options.test.js src/pages/tournament/groups-data.test.js
cd ..\backend
mvn -Dtest=GroupStandingEngineTest test
```

---

## 9. 手动验收清单（端到端 QA）

> 自动化之外，按以下清单对全链路做一轮手工验收（真实或模拟比赛）。

### 创建赛事
- 羽毛球个人赛：粘贴名单（空格/换行/逗号分隔）→ 一键生成签表
- 排球：逐队录入（6~12 人/队，球衣号、队长、自由人标记）→ 校验通过
- 种子排名、轮空（人数非 2 的幂）正确；创建后回赛事详情，对阵图/小组赛入口正常

### 记分与执裁独占锁（多设备并发）
- 记分板加分、追分、封顶、发球权切换符合预期；横屏不错位；撤销（History Stack）正确回退且不撑爆存储
- 两台设备同进一场：后进设备只读，顶部提示"当前比赛正由其他设备操作"
- 持锁设备（主裁）操作正常；只读设备无任何编辑、无写接口调用
- 主裁心跳续期：暂停 <75s 内他机重进仍被锁拦截；暂停 >75s 锁过期后他机可接管
- 主裁结算/退出后锁释放，其他设备可重新执裁；主裁断网导致心跳失败时不误判失锁

### 结束与晋级
- 胜者正确推进到下一场（链表晋级）；半决赛败者正确进入三四名赛
- 小组赛结束后积分榜正确、生成淘汰赛成功；平局时提示"未决并列"，可走人工指定出线（资格覆盖）

### 赛后：战报 + 签名 + 归档
- 战报填裁判/挑边/签名/日期可存草稿；签名板真机手写流畅、保存正常
- "封存战报"后不可再改、不可重启比赛；赛事归档后从"我的→归档"可见、可取消归档

### 边界场景
- 退赛：中途退赛排名怎么算（删除 vs 单场判负），比分展示合理
- 重开：结束错了可重启，重启后战报/阵容状态符合预期
- 断网记分：断网继续记，恢复后同步不重复、不丢失
- 横竖屏切换：记分页强制横屏，返回其他页正常
- 并发操作：同时点生成淘汰赛/同时结算，被锁挡住
- iOS/安卓 + 不同机型：横屏缩放、按钮热区正常
